


module.exports = function(arr) {

  score = 0;
  bullsEyeCount = 0;

  if(Array.isArray(arr) !== true) {
    return new Error(" Not able to calculate your score");
  }
  else {
    // for (var i = 0; i < arr.length; i++){
    //   if typeof i === "number" {
    //   };
  }

  return score;

};



